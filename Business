<!DOCTYPE html>
<html>
<head>
  <title>Business - Walinase Africa Hub</title>

  <style>

    body{
      margin:0;
      font-family:Arial, sans-serif;
      background:linear-gradient(to right, black, #002244, #006633);
      color:white;
    }

    header{
      text-align:center;
      padding:50px;
      background:black;
    }

    header h1{
      color:#00ff99;
      font-size:45px;
    }

    header p{
      font-size:18px;
    }

    .business-section{
      display:flex;
      flex-wrap:wrap;
      justify-content:center;
      gap:25px;
      padding:40px;
    }

    .card{
      background:rgba(255,255,255,0.1);
      width:300px;
      padding:25px;
      border-radius:15px;
      box-shadow:0 0 12px rgba(0,0,0,0.5);
      transition:0.3s;
    }

    .card:hover{
      transform:scale(1.
